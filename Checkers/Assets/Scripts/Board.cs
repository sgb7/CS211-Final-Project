using System.Collections;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Security.Cryptography.X509Certificates;
using UnityEngine;

public class Board : MonoBehaviour
{
    
    
    
    
    
    
    
    /*public CheckerPiece[,] pieces = new CheckerPiece[8, 8];
    public GameObject redCheckerPrefab;
    public GameObject whiteCheckerPrefab;

    private void GenerateBoard()
    {
        for(int i = 0; i < 3; i++)
        {
            for(int j = 0; j < 8; j += 2)
            {
                GeneratePiece(redCheckerPrefab, i, j);
                GeneratePiece(whiteCheckerPrefab, i, j);
            }
        }
    }

    public void GeneratePiece(GameObject obj, int x, int y)
    {
        GameObject go = Instantiate(obj, new Vector3(0, 0, -1), Quaternion.identity);
        CheckerPiece cp = go.GetComponent<CheckerPiece>();
        pieces[x, y] = cp;
    }

    void Start()
    {
        GenerateBoard();
    }*/
}
